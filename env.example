# Server Configuration
PORT=3001
NODE_ENV=development

# Google Places API Configuration
GOOGLE_PLACES_API_KEY=<GOOGLE_PLACES_API_KEY>
GOOGLE_PLACE_ID=<GOOGLE_PLACE_ID>

# Cache Configuration
CACHE_TTL=3600

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (Strategy Pattern)
# The system automatically selects the best available strategy

# Email Service Selection
EMAIL_SERVICE=auto
# EMAIL_SERVICE=sendgrid  # Force SendGrid
# EMAIL_SERVICE=smtp      # Force SMTP
# EMAIL_SERVICE=auto       # Auto-select (default)

# Strategy 1: SendGrid (Highest Priority - if configured)
# SENDGRID_API_KEY=your-sendgrid-api-key
# FROM_EMAIL=your-verified-email@domain.com
# FROM_NAME=GrowWise

# Strategy 2: SMTP Fallback (Gmail/Outlook - if SendGrid not available)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-gmail@gmail.com
SMTP_PASS=your-app-password
FROM_EMAIL=your-gmail@gmail.com
FROM_NAME=GrowWise

# Alternative SMTP Options:
# Gmail: smtp.gmail.com:587
# Outlook: smtp-mail.outlook.com:587
# Yahoo: smtp.mail.yahoo.com:587

# Contact Form Configuration
CONTACT_EMAILS=your-gmail@gmail.com,your-name@domain.com
NOTIFICATION_CHANNELS=email
